name: wt_firepod_tree_view_examples
description: "Test examples for the FirepodListView component"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  animated_tree_view:
  flutter:
    sdk: flutter
  freezed_annotation:
  riverpod:
  wt_firepod_tree_view:
    path: ..


dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  flutter_lints:
  freezed:
  json_serializable:

flutter:
  uses-material-design: true
